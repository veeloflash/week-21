# Project reflection

The main improvement was separating the usable product from seven classroom experiments. Stable document IDs and labelled queries make retrieval quality measurable rather than anecdotal. The remaining limitation is the lightweight local encoder; production work should compare it with a versioned model and a larger labelled corpus.