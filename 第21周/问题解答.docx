1. 企业级 AI 系统架构图（以 ChatGPT 为例）
代码
User
  ↓
Frontend（Web / App）
  ↓
API Gateway
  ↓
Request Handler
  ↓
Embedding Service（文本 → 向量）
  ↓
Vector Database（RAG / Memory）
  ↓
LLM Core（Transformer）
  ↓
Post-processing（格式化 / 安全）
  ↓
Response
  ↓
User
2. Embedding 在系统中的位置
位于 用户输入之后、LLM 推理之前，负责把文本转换成向量。

3. Embedding 的输入
自然语言文本（用户问题、文档内容、对话历史等）。

4. Embedding 的输出
高维语义向量（例如 384、768、1536 维）。

5. 为什么系统必须使用 Embedding？
因为模型只能处理数字，Embedding 能把语义转换成可计算的向量。

6. 去掉 Embedding 会导致哪些功能失效？
搜索、推荐、RAG、语义检索、语义理解等全部无法工作。

7. Sparse Vector 的结构
大部分维度为 0。

8. Dense Vector 的结构
大部分维度为非 0。

9. Sparse Vector 的存储特点
维度高、稀疏、存储简单。

10. Dense Vector 的存储特点
维度较低、紧凑、信息密度高。

11. Sparse Vector 的优点
简单、易理解、计算快。

12. Dense Vector 的优点
能表达复杂语义，适合深度模型。

13. Sparse Vector 的缺点
无法表达复杂语义。

14. Dense Vector 的缺点
训练成本高，难以解释每一维含义。

15. Sparse Vector 的典型应用
TF-IDF、关键词搜索。

16. Dense Vector 的典型应用
LLM、Embedding、推荐系统。

17. 为什么现代 LLM 使用 Dense Vector？
因为 Dense Vector 能表达复杂语义关系。

18. Embedding Pipeline
文本 → Token → Embedding → Vector

19. Embedding Pipeline 每一步作用
文本：原始输入

Tokenization：切成 token

Embedding：token → 高维向量

输出向量：用于检索或模型输入

20. 五个真实产品的 Embedding 用途
Google Search：语义搜索

YouTube：视频推荐

Amazon：商品推荐

ChatGPT：RAG 检索

Spotify：音乐推荐

21. 没有 Embedding 会遇到的问题
同义词、拼写、多语言、上下文全部失效。

22. A=(1,2)、B=(2,3)、C=(8,9) 的二维向量图
代码
|       C
|
|
|
|
|   B
| A
|
|-----------------
23. 哪个点距离 A 最近？为什么？
B，因为 AB = √2，是最小距离。

24. 为什么 Embedding 通常不是二维？
二维无法表达复杂语义，需要更高维度才能编码更多信息。

25. 为什么 Embedding 常见维度是 384、768、1536？
这些维度是在表达能力与计算成本之间的工程折中选择。

26. 维度越高是否越好？为什么？
不一定，高维更强但更慢、更贵。

27. Euclidean Distance 计算方法
(
𝑥
2
−
𝑥
1
)
2
+
(
𝑦
2
−
𝑦
1
)
2
28. Cosine Similarity 计算方法
𝑎
⋅
𝑏
∥
𝑎
∥
∥
𝑏
∥
29. 为什么 Euclidean 与 Cosine 排序可能不同？
一个看距离，一个看方向。

30. 为什么 car 与 automobile 更近？
因为它们是同义词，语义几乎一致。

31. 判断最相近的词组
car 与 automobile。

32. 理由
语义重叠度最高。

33. Keyword Search 效果
只能匹配字面，不懂语义。

34. TF-IDF 效果
看词频，不看语义。

35. Embedding Search 效果
能理解语义。

36. 哪种最好？
Embedding，因为能理解意思。

37. 真实网站为什么不能只用 Keyword Search？
例如 ChatGPT：关键词只能匹配字面，无法理解语义。

38. Embedding 在该网站解决的问题
语义理解、推荐、排序。

39. Embedding 判断错误案例
bank（银行） vs bank（河岸）。

40. 为什么失败？
语境不足导致多义词混淆。

41. 神经元结构图
代码
Input → Weight → +Bias → Activation → Output
42. 每一步负责什么
输入 → 加权 → 加偏置 → 激活 → 输出。

43. 如果 Weight 全部变成 0
模型输出常数，无法学习任何模式。

44. 为什么不能直接把 Study Hours + Homework + Attendance 相加？
不同特征重要性不同。

45. 为什么需要 Weight？
表示每个特征的重要程度。

46. 为什么需要 Bias？
调整整体输出的偏移。

47. Loss 越来越大可能原因
学习率太大、数据问题、模型结构不合理。

48. Learning Rate 太大
Loss 震荡或发散。

49. Learning Rate 太小
训练速度极慢。

50. 数据太少为什么失败
容易过拟合，无法泛化。

51. 数据质量太差为什么失败
噪声太多，模型学到错误模式。

52. 神经元结构图（三个输入）
代码
8 -------- w1 ----\
                   \
9 -------- w2 ------> Σ(xi * wi) + b → Activation → y
                   /
10 ------- w3 ----/
53. 标注 Input、Weight、Bias、Activation、Output
Input：8、9、10
Weight：w1、w2、w3
Bias：b
Activation：ReLU / Sigmoid / Tanh
Output：y

54. 如果所有 Weight 都变成 0
模型无法学习。

55. 如果所有 Weight 都一样
无法区分不同输入。

56. 为什么不能只有 Weight？
需要 Bias 调整整体输出。

57. Bias 的作用
整体偏移。

58. 现实生活中的 Bias
出租车起步价。

59. 没有 Activation Function 会怎样？
模型变成线性模型，无法学习复杂任务。

60. Sigmoid 优点
平滑、可输出概率、适合二分类。

61. Sigmoid 缺点
梯度消失、深层效果差、收敛慢。

62. Sigmoid 常见应用
二分类。

63. ReLU 优点
不易梯度消失、计算快、适合深度网络。

64. ReLU 缺点
可能出现“死 ReLU”。

65. ReLU 常见应用
CNN、Transformer。

66. Tanh 优点
范围更广、中心在 0、表达能力比 Sigmoid 强。

67. Tanh 缺点
容易梯度消失。

68. Tanh 常见应用
旧式 RNN。

69. 为什么真实答案 90、预测 20 时 Loss 很大
误差大。

70. Loss 的作用
告诉模型哪里错了。

71. Loss 一直不下降原因
学习率、数据、模型问题。

72. Loss 一直下降说明
模型在学习。

73. Loss 一直上升原因
学习率太大或数据错误。

74. Loss 震荡原因
学习率太大。

75. Transformer 为什么需要 Weight
表示重要性。

76. Transformer 为什么需要 Bias
偏移。

77. Transformer 为什么需要 Activation
提供非线性。

78. Transformer 为什么需要 Gradient Descent
优化参数。

79. 为什么神经网络基础仍适用于 LLM
LLM 本质是超大规模神经网络。

80. 企业 AI 系统架构图（简版）
代码
User → Input → Embedding → LLM → Output
81. Embedding 所在位置
文本 → 向量阶段。

82. Embedding 输入
文本。

83. Embedding 输出
向量。

84. 为什么必须使用 Embedding
为了语义理解。

85. 去掉 Embedding 会导致哪些功能失效
搜索、推荐、RAG。

86. RAG 工作流程图
代码
Embedding → Vector Store → Top-k → LLM
87. RAG 每一步作用
向量化 → 检索 → 选最相关 → 生成回答。

88. 哪一步使用了 Embedding
检索前。

89. 没有 Embedding RAG 能否工作
不能。

90. 为什么 LLM 需要 Embedding
输入必须是向量。

91. AI Agent 中 Embedding 负责什么
检索、记忆。

92. 去掉 Embedding Agent 会失去什么
语义检索能力。

93. 校园 AI 系统必须使用 Embedding 的模块
搜索、QA、推荐。

94. Embedding 失效案例
多义词 bank。

95. 为什么失败
语境不足。

96. 是数据问题吗
可能。

97. 是模型问题吗
可能。

98. 是算法问题吗
可能。

99. 如何改进
更大模型、加入 RAG。

100. Semantic Search 是否需要 Embedding
需要，因为要理解语义。

101. Recommendation 是否需要 Embedding
需要，因为要计算相似度。

102. RAG 是否需要 Embedding
需要，因为检索依赖向量。

103. LLM 是否需要 Embedding
需要，因为输入必须是向量。

104. AI Agent 是否需要 Embedding
需要，因为记忆与检索依赖向量。

105. 为什么现代 AI 都建立在 Embedding 上
因为 Embedding 是语义基础。

106. 开源项目的 Embedding 调用位置
通常在 encode(text)。

107. 输入是什么
文本。

108. 输出是什么
向量。

109. 为什么需要 Embedding
为了语义检索。

110. 模块作用
向量化文本。

111. Prompt Injection 与 SQL Injection 相同点
都是输入攻击。

112. 最大区别
SQL 是结构化漏洞，Prompt 是语义漏洞。

113. 为什么 Prompt Injection 是 AI 特有漏洞
模型会执行文本语义。

114. 为什么传统 WAF 无法防御
无法理解语义。

115. 哪一步可能被攻击
Input。

116. 哪一步不能被攻击
Embedding。

117. 为什么
Embedding 不执行指令。

118. 公开案例
ChatGPT 系统提示泄露。

119. 漏洞形成原因
模型过度遵从指令。

120. 利用方式
诱导模型泄露。

121. 修补方式
过滤、隔离。

122. 企业最终解决方式
安全层。

123. 五条 Prompt 安全规则
长度限制、敏感词过滤、权限检查、上下文隔离、日志审计。

124. 每条规则防止什么
对应输入绕过、恶意指令、越权、上下文污染、难以追踪。

125. 为什么 Embedding 不执行 Prompt
它只是向量化。

126. 为什么 RAG 仍会被 Prompt Injection
恶意内容可能进入向量库。

127. 简单 Prompt Filter
长度限制 + 特殊字符过滤。

128. Prompt Injection 与 SQL Injection 最大区别
语义 vs 结构。

129. 为什么 Embedding 有效
能表达语义。

130. 为什么 Embedding 会失败
语境不足。

131. 为什么 Cosine Similarity 优于关键词匹配
比较方向，能理解语义。

132. 如何提高相似度质量
更好模型 + RAG。

133. 攻击入口
用户输入。

134. 攻击路径
输入 → 模型 → 输出。

135. 为什么攻击能成功
模型遵从指令。

136. 为什么当前防御有效
过滤 + 限制。

137. 剩余风险
语义绕过。

138. 为什么系统有效
语义检索。

139. 为什么系统会失败
Embedding 错误。

140. 最大漏洞
Prompt Injection。

141. 最大改进空间
更强模型。

142. 如果重新设计
加 RAG、加安全。

143. 整体系统流程图
代码
User → Input → Embedding → Similarity → Ranking → Result
144. 每个模块负责什么
输入 → 向量 → 计算 → 排序 → 输出。